version: 2.1

jobs:
  run-migrations:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout 
      - run:
          name: Install dependencies
          command: |
            sudo apt-get install jq


workflows:
  my_workflow:
    jobs:
      - run-migrations