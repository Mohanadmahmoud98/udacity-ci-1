version: 2.1

jobs:
  run-migrations:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Run migrations
          command: |
            yum install sudo -y
            curl -sL https://rpm.nodesource.com/setup_13.x | sudo bash - 
            yum install -y nodejs
            npm i

workflows:
  my_workflow:
    jobs:
      - run-migrations